import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Contact NIBOG | Parent & Event Support",
  description: "Contact the NIBOG team for event, registration, booking and parent support.",
}

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children
}
